// Shared values reused across multiple mini-games. Keep only constants that
// are genuinely identical across games here — per-game tuning values (like
// gravity or move speed) that differ between games belong in that game's own
// file instead.

export const CANVAS_WIDTH = 800;
export const CANVAS_HEIGHT = 600;
export const PLAYER_WIDTH = 40;
export const PLAYER_HEIGHT = 50;
export const MOVE_SPEED = 5;
